# Phase 2 Macros

Phase 2 intentionally contains no custom production macros.

AutomateDV supplies the Data Vault automation macros.

Custom metadata-driven macros will be introduced after the staging layer is
validated, so that each abstraction is introduced in a controlled sequence.
